# semantic-analyzer-cpp
Semantic Analyzer (C++)  A compiler-style **semantic analyzer** implemented in C++ that enforces **mutable vs immutable variable rules**, performs **type checking**, and detects **semantic errors** using an **AST + Visitor pattern**.  This project focuses on the **semantic analysis phase** of a compiler, separate from parsing and code generation.
